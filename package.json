{
  "name": "agentity",
  "private": true,
  "workspaces": [
    "packages/agentity-sdk-ts",
    "packages/agentity-cli",
    "packages/agentity-inspector",
    "packages/agentity-middleware/typescript",
    "packages/agentity-mcp",
    "packages/agentity-a2a",
    "packages/agentity-manifest-gen"
  ],
  "packageManager": "pnpm@11.1.2",
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "sharp"]
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  }
}
